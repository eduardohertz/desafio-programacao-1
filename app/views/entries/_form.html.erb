<%= simple_form_for(@entry, html: { multipart: true }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= simple_fields_for(@entry_file) do |b| %>
  		<%= b.input :file, as: 'file' %>
  	<% end %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Enviar' %>
  </div>
<% end %>
